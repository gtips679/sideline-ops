import { PlaceholderPanel } from "../../components/PlaceholderPanel";
import { SectionHeader } from "../../components/SectionHeader";

export function InventoryPlaceholder() {
  return (
    <>
      <SectionHeader title="Inventory" />
      <PlaceholderPanel title="Inventory placeholder" description="Inventory photo upload, item tracking, and Zettle-related workflows will be added after the foundation is stable." />
    </>
  );
}
